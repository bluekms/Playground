syntax = "proto3";

option csharp_namespace = "World";

service Foo {
  rpc RequestHandler (FooRequest) returns (FooResult);
}

message FooRequest {
  string Data = 1;
}

message FooResult {
  string Result = 1;
}